# Svelte DevTools completion plan

Audit date: 2026-09-05. Scope: Svelte 5 components and SvelteKit applications on Vite 8, with a useful browser panel and reliable agent access.

This is the active discrepancy register. It replaces the implementation queue in [inspiration.md](../../inspiration.md). The Vue DevTools comparison describes product inspiration, not a promise to reproduce Vue-specific features. A feature is complete only when its behavior works against an instrumented application. Source inspection and tests of copied logic do not establish that result.

## Execution order

1. Establish the build, unit, type-check, and real-browser baseline.
2. Fix component identity, source navigation, state editing, request-to-mock workflows, interception, settings, and resizable layouts in parallel. Assign separate file ownership to reduce conflicts.
3. Exercise the plain Svelte, SvelteKit, Pokédex, and Todo SQLite applications. Compare rendered state with API data.
4. Verify undo and redo after application changes and inspector edits. Verify repeated instances independently.
5. Rebuild the distributed panel, restart the servers, and repeat API and UI checks.
6. Update this register with results. Keep unresolved limits visible in the public documentation.

Priorities: P0 means incorrect application or debugger state; P1 means a broken core workflow; P2 means an incomplete supported workflow or validation gap. “Implemented; verify” means code exists but the final integrated check is pending. “Open” means the discrepancy remains. These labels are not release approval.

## Observed discrepancies

| ID | Priority | Evidence and discrepancy | Expected behavior and action | Validation | Status |
|---|---|---|---|---|---|
| D01 | P0 | Pokédex renders 20 `PokemonCard` instances, but the component registry used a file-level ID and merged them. | Assign an instance ID. Preserve file metadata separately. Address setters, state, and snapshots to the correct instance. | Count 20 distinct cards in API; edit one; check siblings; undo and redo without extra snapshots. | Verified: 20 unique Pokédex instances and per-card inspection; isolated sibling edits and undo/redo in browser regression |
| D02 | P1 | Component Source reports “no source location available” despite a filename. | Use the filename as a fallback at line 1. Open through the local Vite server and report launch failures. | Inspect a real card; invoke open-in-editor; confirm the requested path and line. | Verified: real card filename fallback; configured editor process received canonical path, line, and column |
| D03 | P1 | Component detail has no inline state editor. A store editing helper exists. | Edit writable state from the detail panel. Validate JSON and show failures. Record the change through normal time travel. Keep derived values read-only. | Edit scalar, array, and object values; reject invalid input; verify DOM, API, undo, redo, and sibling isolation. | Verified: scalar/object/array edits, invalid JSON, read-only values, sibling isolation, DOM/API updates, undo/redo |
| D04 | P1 | Network mocks require manual reconstruction of a request. | Prefill an editable mock draft from a captured request. Preserve method and literal URL; use available response data; explain unavailable/truncated bodies. | Capture a real fetch; create mock; change response; repeat fetch; disable rule and confirm real response. | Verified: captured fetch → editable draft → mocked response → disabled rule restores real response |
| D05 | P1 | Initial E2E run passed 1/4 because the SvelteKit server was not started by the suite. | Start both fixture servers and fail explicitly on startup errors. | `npm run test:e2e` from stopped fixture servers. | Verified: both servers start from stopped fixtures; final suite results below |
| D06 | P1 | Todo SQLite used Vite 6, lacked plugin integration, and imported missing CSS packages. | Make it a runnable Vite 8 integration fixture with the DevTools host and runtime. | Open Todo; authorize panel; create/update/delete a todo; inspect server events and component state. | Verified: Todo CRUD, live API caches, component detail, server traces, and panel walkthrough |
| D07 | P1 | No MCP entry point existed for agents. | Provide typed discovery and bounded inspection tools over stdio, using the authenticated HTTP transport. Return actionable readiness and authorization errors. | Real SDK client connects over stdio to a running app; exercise all tools plus stale/unauthorized cases. | Verified: real stdio SDK connection plus typed tool, auth, freshness, session, size-limit, and acknowledged mutation tests |
| D08 | P1 | HTTP runtime data is a panel cache, not a live query. Opening the app alone does not populate it. | Advertise panel dependency and cache age. Reject missing/stale MCP runtime results. A later panel-independent transport must prove runtime connectivity. | Read before panel opens, during sync, and after panel closes; distinguish empty data from unavailable data. | Panel dependency and per-session caches explicit; missing/stale results rejected; independent transport deferred boundary |
| D09 | P1 | `POST /api/set-state` returns 501. There is no server-to-runtime mutation acknowledgement. | Keep API and MCP honest. Implement remote mutation only with a live command channel, acknowledgement, instance targeting, and normal snapshot capture. | API call changes app DOM, reports unknown targets/errors, and supports undo/redo. A cache-only change must fail. | Verified: session-targeted acknowledged mutation changes live DOM and supports undo/redo; failed targets create no snapshots |
| D10 | P1 | `tests/client/store.test.ts` and `tests/runtime/time-travel-store.test.ts` explicitly replicate store logic. | Test production code or real compiled fixtures. Do not treat copied algorithms as regression coverage for capture/restore. | Introduce a production regression and confirm the test fails; retain browser undo/redo checks. | Fixed: copied algorithms replaced with production rune-store tests; caught and fixed real unmount mismatch |
| D11 | P1 | Instance IDs change the setter and snapshot matching contract. | Preserve restore correctness without cooldowns or timestamp gates. Follow the mandatory Spring counter procedure in AGENTS.md. | Record; increment once; expect 2/2; undo 1/2; redo 2/2 and counter 1. Also restore repeated components. | Verified: exact Spring procedure produced 2/2 → 1/2 → 2/2 with counter 1 → 0 → 1; no extra snapshot |
| D12 | P2 | Route scanner strips groups before recording them, merges equal cleaned URLs, and rewrites bracket syntax with regular expressions. | Preserve route identity and group metadata. Parse optional/rest/matched parameters correctly. Do not present a route template as a concrete navigable URL. | Fixtures for `(group)`, `[[optional]]`, `[...rest]`, `[id=matcher]`, and layout-only directories. | Verified: route parser fixtures and live Todo route inventory/current URL |
| D13 | P2 | Route inventory assumes `src/routes`; it is not the active browser route. | Label inventory accurately. Support configured route roots before claiming full Kit route coverage. Track active route separately. | Nondefault routes directory and browser navigation, including back/forward. | Verified: configured routes/base tests and separate live active URL; route templates remain inventory |
| D14 | P2 | Transform include defaults to `.svelte`; standalone `.svelte.ts`/`.svelte.js` rune modules are not instrumented. | Document component-source coverage. Add module/store instrumentation only with explicit ownership and lifecycle semantics. | A shared rune module consumed by two components; distinguish shared state from per-instance state. | Documented boundary: component-owned runes only; shared module ownership/instrumentation deferred |
| D15 | P2 | `enableStateInspection` is read but unused in the plugin. | Honor `false` or remove the option in a versioned API change. | Compile same fixture with option true/false and inspect transformed code and live behavior. | Fixed; transform option regression passes |
| D16 | P2 | Runtime public `getTimeline`, `subscribe`, and `trace` are empty/no-op compatibility surfaces. | Implement their advertised behavior or identify them as unsupported. | Subscribe to a real state change; unsubscribe; check timeline and explicit trace. | Fixed; production runtime subscriptions/history/trace tests pass |
| D17 | P2 | Dashboard/settings/assets/graph had presentation and behavior gaps. | Use consistent visual hierarchy, readable empty states, functioning theme/scale/motion controls, and useful graph selection. | Review real panel at narrow and wide sizes, light/dark themes, and keyboard-only interaction. | Verified: live wide/narrow panels, Huge font, keyboard selection, Settings persistence and reset |
| D18 | P2 | Asset panel is browser resource timings, not a filesystem asset explorer. | Label it Asset Timings and inspect resources from the application context. Keep filesystem exploration outside current claims. | Load an image/script in the app; confirm its URL and timing in the panel. | Verified: Todo app resource timings and detail at wide/narrow sizes |
| D19 | P2 | Broad “Svelte 5” and “latest” compatibility claims lack a lower-bound matrix. Instance identity uses `$props.id()` (Svelte 5.20+). | Declare the required Svelte floor and verify it alongside the current version. Test Vite 8/Rolldown and both host authorization variants used by fixtures. | Consumer builds at declared minimum/current versions; plain and Kit real-browser runs. | Verified: Vite 8.2.2/host 0.4.8/current Svelte browser fixtures; Svelte 5.20 client+SSR compiler fixtures. Full minimum-version runtime matrix remains untested |
| D20 | P2 | `apply: 'serve'` proves plugin exclusion, but does not alone prove consumer production output contains no hook/runtime artifacts. | Inspect production builds of configured plain and Kit applications. Avoid a blanket claim based only on the plugin flag. | Build both fixtures; search emitted assets for injected globals, dock scripts, and development routes. | Plain/Kit production builds and response checks pass; noop fetch side effect fixed and regression-tested |
| D21 | P2 | Baseline Svelte check reported 24 warnings. Browser coverage is Chromium only. | Review warnings for accessibility and reactive-state defects. Validate keyboard access, focus, labels, contrast, and overflow. | `npm run check`; keyboard panel walkthrough; narrow viewport. Safari/Firefox remain explicitly untested. | Verified: zero Svelte check errors/warnings and live keyboard/overflow review. Safari and Firefox remain untested |
| D22 | P2 | No measured performance budget is established. MCP pagination trims results only after fetching the full HTTP cache; individual values remain unbounded. | Bound expensive output and measure many-component/event workloads before claiming scalability. | Large component list and large state payload; measure response size, panel responsiveness, and cleanup after unmount. | Verified: bounded MCP output and live 1,000-component mount/update/unmount. Local timing below; no general performance guarantee |
| D23 | P2 | Documentation says four packages and runs E2E through Vitest. Feature comparison and editor claims overstate completion. | List five packages, use Playwright, document MCP and cache limits, and link this register as the current status. | Compare commands and references against package scripts and live checks. | Updated in this audit |
| D24 | P2 | Mock rules apply to browser fetch interception; server traces do not establish server-side mocking. | Explain scope in UI/docs. Avoid silently claiming SSR requests can be intercepted by client rules. | Compare a browser fetch with SSR load fetch against the same endpoint. | Verified scope: browser fetch mocks; SSR trace streaming regression. Native XHR is preserved and is not mocked |
| D25 | P2 | Source lookup and editor launch cross a filesystem boundary. Existing source API is constrained to project root. | Keep editor requests constrained too; return clear errors for missing files and out-of-root paths. | Valid file, missing file, traversal, and symlink outside root. | Verified: real configured editor launch plus missing/out-of-root/traversal/symlink rejection tests |
| D26 | P1 | User reports truncated Network request details with no way to enlarge the pane. Other inspection splits need the same audit. | Use the shared resizable split primitive across inspection panels. Support pointer/keyboard resizing, bounded dimensions, deliberate scroll areas, and narrow layouts. | Enumerate consumers; resize each with pointer and keyboard; inspect long bodies/URLs at narrow and wide widths with larger fonts. | Verified: all shared split consumers; live pointer/keyboard detail resize and wide/narrow/Huge-font walkthrough |
| D27 | P1 | User reports that Settings controls do not work. Existing presentation does not establish application or persistence of preferences. | Make each exposed preference affect its intended surface immediately. Persist and restore supported preferences. Remove or label inactive options. | Toggle every setting in the live panel; verify theme, system changes, font scale, and reduced motion; reopen and check persistence. | Verified: theme/system/scale/reduced motion, persistence, reload and reset in real browser tests |
| D28 | P1 | User reports mock rules are not applied to actual requests. Saving rules in the panel does not prove runtime interception. | Wire rule delivery to the live browser interception path. Preserve pass-through when disabled. Report scope and failures clearly. | Create a rule from a captured request; repeat the real app request; verify changed response/DOM; disable and confirm real response. Test supported fetch/XHR paths separately. | Verified: actual browser fetch mock/disable; infrastructure requests bypass mocks; native XHR preserved; stream/abort regressions pass |
| D29 | P2 | No shared design guideline explains layout, settings, error, source, and agent interaction contracts. | Record concrete UI and agent guidelines plus architectural decisions. Separate design intent, implementation, and verified results. | Cross-check documents against production contracts and final live checks; retain open discrepancies. | Verified: guidelines and proposed ADRs; live Todo MCP guide trial discovered session, listed metadata, and inspected one instance; 29 local links resolve |
| D30 | P0 | Independent review found module-only rune scripts instrumented with missing instance identifiers and const destructuring assigned by generated setters. | Instrument only instance scripts and preserve every binding's const/derived metadata. | Compile client and SSR fixtures for module-only, aliases, defaults, array/rest/nested patterns. | Fixed; production compiler tests pass |
| D31 | P0 | Fetch tracing awaited complete cloned bodies; streaming fetches could hang and previews were unbounded. | Return the native response promptly; capture bounded previews asynchronously; clean up readers. | Real ReadableStream tests for SSE, large bodies, cancellation and errors. | Fixed for browser and Kit; focused tests pass |
| D32 | P1 | Production store tests exposed incompatible unmount payload names; dead components remained in the panel. | Normalize unmount messages and remove bridge tracking. | Production bridge/store mount→state→unmount test. | Fixed; production tests pass |
| D33 | P1 | User reports +layout is disconnected from +page in the graph. DOM-only parent discovery misses components without wrappers. | Record component context ancestry; use DOM only as fallback. | Real Kit/Pokédex layout→page→card ancestry, including no-wrapper layouts. | Verified: real Pokédex layout → page → all 20 cards, including the layout without a DOM wrapper |
| D34 | P1 | User still sees only one PokemonCard through element inspection; first-element-only tagging misses other roots/branches. | Tag owned native nodes using the compiler AST and keep per-instance IDs. Refresh served artifacts before verification. | Inspect multiple cards, alternate branches and multi-root components; verify selected IDs differ. | Verified: each of 20 real Pokédex cards selects its own component and props; AST multi-root/branch tests pass |
| D35 | P0 | Function-valued state can seed an editor with null; JSON previews are not safe restore values for functions, Map, Set and other non-JSON state. | Use actual live values for editability. Mark non-JSON values read-only; preserve them during restore. | Functions/nested functions/Map/Set/cycles/undefined regression tests plus real callback prop inspection. | Verified: non-JSON runtime/restore regressions and real callback prop inspection. Functions display safely and remain read-only |
| D36 | P1 | Published-package scripts omit MCP, rewrite dependency versions and use inconsistent order/gates. | Use one dependency-ordered workflow with build/type/unit/browser/pack gates and explicit publish action. | Stubbed dry-run/failed-gate/publish-order checks and real five-package pack gate. | Fixed; validation passes; no packages published |
| D37 | P1 | Supported host 0.4.8 uses six-digit authorization and changed dock entry markup. Legacy auth scripts cannot open the panel. | Test the supported host's actual dialog and entry flow. Update runnable docs and mandatory Spring procedure. | Authenticate fresh browser sessions and run full Chromium suite. | Verified: fresh six-digit authorization and dock entry discovery across browser acceptance scripts |

| D38 | P0 | Initial panel sync passed live snippets, BigInt, and cycles into JSON conversion; inspecting Todo layout could throw `snippet_without_render_tag`. | Use one display serializer that never invokes functions or getters. Keep live editability checks separate. | Production bridge/store serialization regressions; real Todo layout/Components walkthrough. | Fixed and verified |
| D39 | P1 | A 552,896-byte sync exceeded the beacon queue limit. The runtime had 1,000 instances but the API cache stayed empty. | Use authenticated fetch with one request in flight and a bounded request deadline. Retry on the next periodic sync. | Production payload above 64 KiB and live 1,000-instance API sync. | Fixed and verified |
| D40 | P1 | Browser interception replaced native XHR and wildcard mocks could intercept DevTools infrastructure. | Install fetch-only interception; bypass local DevTools routes; preserve abort behavior. | Runtime network boundary regressions and browser mock workflow. | Fixed and verified |
| D41 | P1 | Updated props reached runtime state but the panel retained their initial props values. | Mark prop updates from instance metadata. Update both state and props in the panel. | Production runtime/store regressions; all 1,000 live instances receive the updated prop. | Fixed and verified |
| D42 | P0 | Anonymous sync could overwrite the latest named session; older servers could ignore explicit MCP session selection. | Isolate anonymous caches. Reject invalid IDs and mismatched or missing response session IDs. | HTTP session-isolation tests and MCP SDK mismatch tests. | Fixed; independent review and 69 focused HTTP/MCP tests pass |
| D43 | P1 | Removing 1,000 components while the Components panel was open took 53 seconds. | Remove repeated full-tree scans and measure runtime, API, and panel cleanup. | Repeat the same live stress workflow; report measured timings without claiming a general benchmark. | Fixed: same 1,000-instance workflow improved from 53,279 ms to 1,193 ms; original five-second assertion restored |
| D44 | P1 | A Spring removed before settlement left motion bookkeeping behind and prevented all later state captures. | Clear motion and queued-state entries for the destroyed instance. | Production regression fails with one snapshot before the fix and passes with two after it; live continuation regression. | Verified: production red/green regression plus actual Spring unmount, later survivor edit, and Undo in Chromium |
| D45 | P2 | Closing a panel retained parent-window bridge listeners and polling callbacks. | Dispose listeners and timers when the panel leaves. | Regression verifies teardown and no stale sync delivery. | Verified: parent listeners, polling timers, and subscriptions are released; back-forward cache is preserved; production regression passes |

## Verification record

The initial repository baseline reported 455 unit tests passing, a successful build, and zero Svelte check errors with 24 warnings. The first E2E run exposed the missing Kit server. After its setup was fixed, all four existing E2E tests passed. These results precede the instance/state/source/network changes and do not certify them.

Real-app observations include the Pokédex instance collapse and the Todo fixture integration failures. The MCP adapter has been exercised through real stdio against the plain Svelte application. The final build passes all five workspaces. The integrated validation below uses those built artifacts.

## Release boundary

Do not close this plan solely because unit tests pass. D01–D04 and D11 require application DOM, authenticated API, and panel verification. Keep open items visible. The user authorized branches, commits, pushes and PRs. Publish no npm release without a release instruction. Do not declare full Vue DevTools parity. Keep deferred capability boundaries distinct from unfinished fixes.

### Measured large-tree workflow

The real Vite fixture mounts 1,000 instrumented `StressItem` components. Chromium opens the authorized panel, searches the tree, selects a component with the keyboard, updates every prop, and removes every instance. Runtime, HTTP cache, and panel all end with zero fixture instances. The metadata response contains 187,035 bytes. The accepted run measured 3,552 ms for mount/authorization/panel/sync, 1,992 ms for update/sync, 35 ms for keyboard selection, and 1,193 ms for unmount/sync. Before the tree index fix, the same teardown took 53,279 ms. These are local observations, including automation and sync overhead. They are not a cross-machine performance budget.

Run `node scripts/verify-stress.mjs` after building packages. The script uses port 5180 and cleans up its own server.

### Analyzer review completed

After the user approved the final review, the official `@sveltejs/mcp` 0.1.26 analyzer checked all 27 changed Svelte components and rune modules. Source analysis ran locally inside the network-restricted sandbox, using a cached public documentation catalog. No source was uploaded. This corrects the earlier assumption that the analyzer required external source transmission.

The review reports two contextual lint issues and 46 advisory suggestions, not an issue-free report. An independent reviewer checked the affected code. The review also found unbounded Network client history and clear markers; 0.1.1 fixes that retention gap. See the [review decisions and raw results](../../validation/svelte-autofixer-review.md).

### Final integrated validation

- `npm run build`: all five workspaces pass, with the built panel served from `dist/`.
- `npx vitest run`: 528 tests pass in 33 files. These include production rune stores, bridge disposal, parser/compiler fixtures, runtime lifecycle, streaming/abort boundaries, HTTP session isolation, and MCP SDK behavior.
- `npm run check`: zero errors and zero warnings.
- `npm run test:e2e`: 11 Chromium tests pass in 43.3 seconds. Coverage includes plain Svelte and SvelteKit, real MCP stdio, scoped state edits and failed targets, undo/redo, non-JSON values, request-to-mock interception/disable, Settings persistence, resizing, and recording after a Spring unmounts.
- `npm run release:check`: all five package manifests, packed entry points, and distributed artifacts pass. No npm package was published.
- `node scripts/verify-editor.mjs`: a real Vite server launches the configured local recorder editor with the correct canonical filename, line 3, and column 2. Missing and out-of-root paths are rejected.
- `node scripts/verify-pokedex.mjs`: all 20 real application card instances are selected independently; every card points to the route page, which points to the layout. Source actions and wide/narrow screenshots are verified. External Pokémon data and images use deterministic test responses.
- Todo SQLite acceptance: create, toggle, edit, and delete a test todo; confirm its cleanup; verify component/timeline/server-event/route APIs; inspect ten panels at 1,440- and 650-pixel browser widths with large fonts and resizable details. The three-call read-only MCP guide trial selects an explicit session and mounted instance.
- Consumer production builds: plain Svelte and SvelteKit outputs exclude injected runtime/dock artifacts. Kit development routes return 404 in production. The no-op server import preserves global fetch.

Compatibility evidence covers the current fixture runtime and Svelte 5.20 client/SSR compilation. It does not establish a full minimum-version runtime matrix, Safari/Firefox support, standalone rune-module instrumentation, or Vue DevTools feature parity. Keep D08 and D14 as explicit future capability boundaries. This register remains at the requested pending-plan path so those boundaries remain discoverable.

The final post-cleanup stress run also passes with the original five-second lifecycle assertion: 3,560 ms mount/authorization/sync, 1,077 ms update/sync, 31 ms keyboard selection, and 2,031 ms unmount/sync. The difference from the 1,193 ms causal comparison includes the periodic sync interval. No fixture instances remain in the runtime, API, or panel.

`node scripts/verify-time-travel.mjs` passes against the final build and the actual SvelteKit Spring counter: record 2/2 at 1; undo 1/2 at 0; redo 2/2 at 1. The required settlement waits are unchanged and no phantom snapshot appears.

### Visual review evidence

Captured from the final built panel against the Pokédex fixture:

- [Component graph with layout/page ancestry and 20 cards](../../images/0.1.0/component-graph.png)
- [Narrow Network view with resizable request detail and mock action](../../images/0.1.0/network-narrow.png)

### Follow-up discrepancy D46

NetworkDesk appended client requests without applying the server path's 500-row cap. Clear-history markers could also accumulate forever. Version 0.1.1 uses a shared bounded history for client and server requests. It expires dismissed IDs when they leave retained source buffers and prevents overlapping server polls. Production regressions exercise client-only, server-only, mixed, and repeated-clear workloads.

Follow-up validation: all five workspace builds pass; Svelte check reports zero errors and warnings; 532 tests pass in 34 files with `npx vitest run --maxWorkers=2`; all 12 browser tests pass. The new browser regression makes 525 real requests, confirms a 500-row cap and authenticated API visibility, clears the panel across two polls, and verifies only the subsequent new request appears. All five package dry runs pass. The first concurrent analyzer/unit/check run caused two existing large-store tests to exceed their five-second limit; the bounded-worker rerun passes without changing timeouts. CI uses the same worker bound.
